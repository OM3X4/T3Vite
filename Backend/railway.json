{
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "npm install && npm prisma generate",
        "startCommand": "node src/index.js"
    },
    "deploy": {
        "dir": "Backend"
    }
}