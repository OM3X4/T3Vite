{
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "npm install && npx prisma generate",
        "startCommand": "node src/index.js"
    },
    "deploy": {
        "dir": "Backend"
    }
}